<Window x:Class="Soft.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:mvvm="clr-namespace:Soft.MVVM"
        d:DataContext="{d:DesignInstance Type={x:Type mvvm:MainViewModel}}"
        mc:Ignorable="d"
        Title="Auto-Documentum" Height="450" Width="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="25"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        
        <mvvm:TopBarUserControl DataContext="{Binding TopBarViewModel}"/>

        <ContentControl Content="{Binding }" ContentTemplateSelector="{StaticResource MainContentTemplateSelector}"/>
    </Grid>
</Window>
